## Overview
This repo implements autoencoders with example notebooks.

## How to run
- Requirements: Python 3.8+, create a virtual environment.
- Install: python -m venv venv && source venv/bin/activate && pip install -r requirements.txt
- You run ipynb notebook.